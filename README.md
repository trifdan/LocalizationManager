# Laravel Inertia React Localization String Manager


### Requirements ###
Stop mysql service
```
service mysql stop
```
Set sail alias 
```
alias sail='sh $([ -f sail ] && echo sail || echo vendor/bin/sail)'

```

### Installation ###
sail up -d
sail artisan migrate
sail npm install && sail npm run dev

